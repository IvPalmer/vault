---
phase: 01-ui-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - FinanceDashboard/styles.py
  - FinanceDashboard/dashboard.py
autonomous: true

must_haves:
  truths:
    - "Dashboard has clean, minimalistic visual design"
    - "Typography uses consistent Inter font throughout"
    - "Spacing is generous and consistent (no cramped layouts)"
    - "Color palette is cohesive (greens for positive, reds for negative)"
    - "Cards and containers have subtle shadows and rounded corners"
  artifacts:
    - path: "FinanceDashboard/styles.py"
      provides: "Complete design system CSS"
      contains: "--spacing-"
      min_lines: 150
  key_links:
    - from: "FinanceDashboard/styles.py"
      to: "FinanceDashboard/dashboard.py"
      via: "apply_custom_styles() call"
      pattern: "apply_custom_styles"
---

<objective>
Create a polished, minimalistic design system for the dashboard with consistent typography, spacing, and color palette.

Purpose: The current UI is functional but "amateur-looking" per PROJECT.md. A cohesive design system establishes visual consistency across all components and gives the dashboard a professional, modern feel.

Output: Comprehensive CSS design system with tokens for colors, spacing, typography, and component styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ui-foundation/01-CONTEXT.md

# Current styles
@FinanceDashboard/styles.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand design system tokens</name>
  <files>FinanceDashboard/styles.py</files>
  <action>
Expand the CSS :root variables to create a complete design token system:

**Spacing tokens:**
```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 32px;
--spacing-2xl: 48px;
```

**Typography scale:**
```css
--font-size-xs: 0.75rem;   /* 12px - labels */
--font-size-sm: 0.875rem;  /* 14px - secondary text */
--font-size-base: 1rem;    /* 16px - body */
--font-size-lg: 1.125rem;  /* 18px - subheadings */
--font-size-xl: 1.25rem;   /* 20px - section titles */
--font-size-2xl: 1.5rem;   /* 24px - page titles */
--font-size-3xl: 2rem;     /* 32px - hero */

--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;

--line-height-tight: 1.25;
--line-height-normal: 1.5;
--line-height-relaxed: 1.75;
```

**Semantic colors:**
```css
--color-positive: #16a34a;     /* Green - income, success */
--color-positive-bg: #dcfce7;
--color-negative: #dc2626;     /* Red - expenses, errors */
--color-negative-bg: #fee2e2;
--color-warning: #ea580c;      /* Orange - installments, alerts */
--color-warning-bg: #ffedd5;
--color-neutral: #6b7280;      /* Gray - secondary info */
--color-neutral-bg: #f3f4f6;
```

**Shadows:**
```css
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.05);
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
```

Keep existing variables but rename for consistency where needed.
  </action>
  <verify>
Read styles.py and confirm all token categories are defined.
  </verify>
  <done>
Design tokens cover spacing, typography, colors, and shadows with consistent naming.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply design system to core elements</name>
  <files>FinanceDashboard/styles.py, FinanceDashboard/dashboard.py</files>
  <action>
Update CSS rules to use the new tokens and polish core elements:

**In styles.py:**

1. Global spacing:
```css
.stApp {
    padding: var(--spacing-lg);
}

/* Section margins */
h3 {
    margin-top: var(--spacing-xl);
    margin-bottom: var(--spacing-md);
}
```

2. Card styling (metrics, containers):
```css
.stMetric {
    padding: var(--spacing-md);
    border-radius: var(--border-radius);
    box-shadow: var(--shadow-sm);
}
```

3. Clean up the vault-title (remove text-shadow, use cleaner styling):
```css
.vault-title {
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-bold);
    color: var(--accent-color);
    letter-spacing: -0.02em;
    margin-bottom: var(--spacing-lg);
}
```

4. Amount styling for tables (green positive, red negative):
```css
.amount-positive {
    color: var(--color-positive);
    font-weight: var(--font-weight-semibold);
}
.amount-negative {
    color: var(--color-negative);
    font-weight: var(--font-weight-semibold);
}
```

**In dashboard.py:**

1. Update the vault-title markup to remove text-shadow effect
2. Remove any inline styles that conflict with the design system
3. Ensure _metric_html function uses design tokens for colors
  </action>
  <verify>
Run `streamlit run FinanceDashboard/dashboard.py` and visually inspect:
- Spacing feels generous and consistent
- Typography hierarchy is clear
- Colors are cohesive
- No cramped or cluttered areas
  </verify>
  <done>
Dashboard has professional, minimalistic appearance with consistent spacing, typography, and colors throughout.
  </done>
</task>

</tasks>

<verification>
1. Visual inspection shows clean, modern design
2. Spacing is consistent across sections (no cramped areas)
3. Typography has clear hierarchy (titles > sections > body > labels)
4. Colors are semantic (green=positive, red=negative, orange=warning)
5. No inline styles that override the design system
</verification>

<success_criteria>
- Design system has complete tokens for spacing, typography, colors, shadows
- All core elements use design tokens consistently
- Dashboard looks polished and professional
- Visual hierarchy is clear
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-foundation/01-02-SUMMARY.md`
</output>
